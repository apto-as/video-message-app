fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
httpx==0.25.2
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Rate limiting and caching
slowapi==0.1.9  # FastAPI rate limiting middleware
redis==5.0.1
aiohttp==3.9.1
psutil==5.9.8  # システムリソース監視用

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
rembg==2.0.67
onnxruntime==1.21.1
pillow>=10.0.0,<11.0.0
opencv-python-headless==4.10.0.84
numpy>=1.24.0,<2.0.0
aiofiles==23.2.1
mutagen==1.47.0
structlog==24.1.0
python-json-logger==2.0.7

# VOICEVOX依存関係のみ（OpenVoiceはネイティブサービスに分離済み）
# 基本的な音声処理ライブラリのみ維持
soundfile>=0.12.0

# 音声処理ユーティリティ
pydub>=0.25.0
ffmpeg-python>=0.2.0

# Prosody調整エンジン用
librosa>=0.10.0
resampy>=0.4.2  # librosaの高品質リサンプリング用

# Mac環境用追加依存関係
scipy>=1.7.0

# YOLOv8 Person Detection (Phase 2)
# Note: torch and torchvision are installed in Dockerfile with CUDA support
# torch==2.0.0+cu118 (installed via Dockerfile)
# torchvision==0.15.1+cu118 (installed via Dockerfile)
ultralytics==8.0.196
face-alignment==1.4.1

# LaMa Inpainting for clothing repair (Phase 1.5)
# Repairs white/light clothing damaged by rembg background removal
simple-lama-inpainting>=0.1.0

# MediaPipe for blink animation post-processing
# Used for eye region detection in BlinkProcessor
mediapipe>=0.10.0,<=0.10.18
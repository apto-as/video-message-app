# Security dependencies for file upload validation

# MIME type detection (magic number based)
python-magic==0.4.27

# Image processing (for PIL)
Pillow==10.1.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
